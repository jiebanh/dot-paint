{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build dot-paint-vscode",
      "type": "shell",
      "command": "pnpm --filter dot-paint-vscode run build",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": []
    }
  ]
}
