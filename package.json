{
  "name": "dot-paint",
  "version": "0.1.0",
  "description": "A browser-based dot painting (pixel art) tool with an editable, index-based color palette.",
  "private": true,
  "license": "MIT",
  "scripts": {
    "test": "node --test test/*.test.js"
  }
}
